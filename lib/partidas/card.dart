
import 'package:flutter/cupertino.dart';

class Card extends StatelessWidget {
  const Card({
    this.width = const Color(0xFF2DBD3A),
    this.height,
  });
  final width;
  final height;
  @override
  Widget build(BuildContext context) {
    return Container(
      height: this.height,
      width: this.width,
      child: ,
    );
  }
}